import * as React from 'react'

export interface INotificationSectionProps {
  placeholder?: null
}

export default function NotificationSection(
  props: INotificationSectionProps,
): JSX.Element {
  return (
    <div>
      <div>ds</div>
    </div>
  )
}
